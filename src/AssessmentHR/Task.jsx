import React from "react";
import PropTypes from "prop-types";
import css from "./Task.scss";

const Task = props => {
	return <div className={css.index}>{"Task"}</div>;
};

Task.propTypes = {};

Task.defaultProps = {};

export default Task;
